# استكشاف الأخطاء وإصلاحها - الخرائط لا تظهر

## ✅ الخطوات المطلوبة لتفعيل الخرائط

### 1️⃣ تأكد من تفعيل APIs في Google Cloud Console

يجب تفعيل هذه الـ APIs:

1. اذهب إلى: https://console.cloud.google.com/apis/library
2. ابحث وفعّل كل من:
   - ✅ **Maps JavaScript API**
   - ✅ **Geocoding API** 
   - ✅ **Places API** (اختياري)

### 2️⃣ تحقق من إعدادات API Key

1. اذهب إلى: https://console.cloud.google.com/apis/credentials
2. اضغط على API Key الخاص بك
3. في قسم "Application restrictions":
   - اختر **"None"** للاختبار المحلي
   - أو أضف `http://localhost:3000/*` في HTTP referrers
4. في قسم "API restrictions":
   - اختر **"Don't restrict key"** للاختبار
   - أو اختر APIs المحددة وأضف Maps JavaScript API

### 3️⃣ تحقق من رصيد Billing Account

- الخرائط تحتاج **Billing Account** مفعّل
- اذهب إلى: https://console.cloud.google.com/billing
- تأكد من وجود بطاقة ائتمان صالحة
- Google تعطي $200 مجاناً شهرياً

### 4️⃣ أعد تشغيل السيرفر

بعد إضافة `.env.local`، يجب إعادة التشغيل:

```bash
# أوقف السيرفر الحالي (Ctrl+C)
# ثم شغّل من جديد:
npm run dev
```

### 5️⃣ تحقق من Console Errors

افتح DevTools (F12) واذهب لتبويب Console:
- ابحث عن أخطاء Google Maps
- إذا وجدت "This API project is not authorized..."
  → المشكلة في تقييدات API Key
- إذا وجدت "REQUEST_DENIED"
  → APIs غير مفعلة أو Billing غير مفعّل

## 🔍 اختبار سريع

### اختبار 1: تحقق من API Key في المتصفح

1. افتح: http://localhost:3000/saudi/riyadh/moving
2. اضغط F12 → Console
3. اكتب:
```javascript
console.log(process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY)
```
4. إذا ظهر `undefined` → المشكلة في `.env.local`

### اختبار 2: اختبار API Key مباشرة

افتح هذا الرابط في المتصفح:
```
https://maps.googleapis.com/maps/api/js?key=AIzaSyBPv4Zmi7Cr8kQmtQF9GzGCLg5NznMfEIs&callback=console.log
```

إذا ظهرت رسالة خطأ:
- "API key not valid" → المفتاح غير صحيح
- "This API project is not authorized" → تحتاج تفعيل APIs
- "REQUEST_DENIED" → Billing غير مفعّل

## 🛠️ الحلول الشائعة

### المشكلة: الخريطة لا تظهر بعد التحميل

**الحل:**
1. تأكد من تفعيل Maps JavaScript API
2. تأكد من وجود Billing Account
3. انتظر 5 دقائق بعد تفعيل APIs (التفعيل يأخذ وقت)

### المشكلة: رسالة خطأ "For development purposes only"

**الحل:**
- هذا طبيعي في Development Mode
- لن تظهر في Production بعد تفعيل Billing

### المشكلة: الخريطة رمادية فارغة

**الحل:**
1. افتح Console (F12)
2. ابحث عن الخطأ
3. غالباً: Billing غير مفعّل أو APIs غير مفعلة

### المشكلة: "This page can't load Google Maps correctly"

**الحل:**
1. تحقق من API Key Restrictions
2. أضف `localhost` في HTTP referrers
3. أو اختر "None" للاختبار

## 📞 الدعم الفوري

إذا استمرت المشكلة، أرسل screenshot من:
1. Console errors (F12 → Console)
2. Network tab (F12 → Network → فلتر: maps)
3. صفحة Google Cloud Console → APIs & Services

## ✨ التأكد من نجاح التفعيل

عند نجاح التفعيل، يجب أن ترى:
- ✅ خريطة تفاعلية كاملة
- ✅ علامة موقع المدينة
- ✅ دائرة نطاق الخدمة
- ✅ أزرار Zoom والتحكم
- ✅ لا توجد أخطاء في Console


